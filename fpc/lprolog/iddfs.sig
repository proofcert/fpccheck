sig iddfs.

% For now, using atomic for nonneg, maybe indexes
%accum_sig cert.
accum_sig atomic.

type   startiddfs   cert.
type   iddfs        nonneg -> cert.

%type   idxatom    idx.
%type   idxlocal   idx.

end
