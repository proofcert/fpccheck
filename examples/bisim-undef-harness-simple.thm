#include "../kernel/logic.thm".
#include "../kernel/cert-sig.thm".
#include "../fpc/simple-fpc.thm".
#include "bisim-undef-examples-sig.thm".
#include "../kernel/kernel.thm".
#include "bisim-undef-examples.thm".

#assert bisim_refl (induction (ss zz) (ss zz) zz (ss zz) zz).

#assert bisim_symm (induction (ss (ss (ss (ss zz)))) zz (ss zz) zz (ss zz)).

#assert bisim_trans (induction (ss (ss (ss (ss (ss (ss zz)))))) zz (ss (ss zz)) zz (ss (ss zz))).
