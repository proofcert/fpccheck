Define nu_trivial : cert -> prop by
	nu_trivial Cert := exists Sim,
		sim Sim /\
		prove Cert (imp (Sim (arg@ q0 (arg@ p0 argv))) (Sim (arg@ q0 (arg@ p0 argv)))).

Define q0_sim_p0 : cert -> prop by
	q0_sim_p0 Cert := exists Sim,
		sim Sim /\
		prove Cert (Sim (arg@ q0 (arg@ p0 argv))).
