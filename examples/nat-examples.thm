Define exists_nat_f : bool -> prop by
	exists_nat_f F := exists IsNat,
		is_nat IsNat /\ F =
		(some N\ IsNat (arg@ N argv)).

Define exists_nat : cert -> prop by
	exists_nat Cert := exists ExistsNat,
		exists_nat_f ExistsNat /\ prove Cert ExistsNat.
